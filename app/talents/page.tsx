export default function TalentsPage() {
  return (
    <main style={{
      fontFamily: "var(--font)",
      fontWeight: 300,
      fontSize: 11,
      color: "#000",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      height: "100vh",
      letterSpacing: "0.04em",
    }}>
      {/* Replace this with your Talents content when ready */}
      talents
    </main>
  );
}
