export default function AccountPage() {
  return (
    <main style={{
      fontFamily: "var(--font)",
      fontWeight: 300,
      fontSize: 11,
      color: "#000",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      height: "100vh",
      letterSpacing: "0.04em",
    }}>
      {/* Firebase auth + profile — coming soon */}
      account
    </main>
  );
}
